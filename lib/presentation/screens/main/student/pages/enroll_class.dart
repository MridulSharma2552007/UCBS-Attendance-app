import 'package:flutter/material.dart';

class EnrollClass extends StatelessWidget {
  const EnrollClass({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
